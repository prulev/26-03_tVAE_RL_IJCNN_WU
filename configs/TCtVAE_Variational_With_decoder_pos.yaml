# This is the config file for TC-tVAE, copied from configs/explore_latent_with_decoder_pos.yaml, Ziyi 260407

# explore latent dimension with tVAE by run.py
MODEL:
  VARIATIONAL: TRUE
  DECODER_POS: TRUE

TRAIN:
  CHECKPOINT_INTERVAL: 1000  # no checkpoint needed
  VAL_DRAW_INTERVAL: 4  # no need for plots during training